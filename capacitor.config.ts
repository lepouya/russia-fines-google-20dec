import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.github.lepouya.russiaFinesGoogle20Dec",
  appName: "russia-fines-google-20dec",
  webDir: "dist",
  server: {
    androidScheme: "https",
  },
};

export default config;
